using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FrameUpdateChecker : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Starting FrameUpdateChecker ...");        
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("New frame in FrameUpdateChecker");
    }
}
